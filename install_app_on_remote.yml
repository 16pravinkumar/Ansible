---
- name: Install and start the nginx
  hosts: all
  become: true
  tasks:
  - name: Install nginx
    apt:
      name: nginx
      state: present
    
  - name: Start the service
    service: 
      name: nginx
      state: started
      enabled: true
